{
  "name": "glsdk-project",
  "version": "1.0.0",
  "scripts": {
    "install-server": "cd loopback/ && npm install",
    "install-webapp": "cd webapp/ && npm install",
    "install": "npm run install-server && npm run install-webapp",
    "start-loopback": "cd loopback/ && npm start",
    "start-webapp": "cd webapp/ && npm start",
    "fireloop": "fireloop"
  },
  "keywords": [
    "fireloop",
    "Grand Lyon"
  ],
  "author": "Clever Age",
  "license": "MIT",
  "devDependencies": {
    "@mean-expert/fireloop": "^1.0.0-beta.2.7"
  }
}
