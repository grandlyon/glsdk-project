{
  "name": "glsdk-project",
  "version": "1.0.0",
  "scripts": {
    "init": "git submodule update --init --recursive && git submodule foreach git checkout master && git submodule foreach git pull origin master",
    "install-server": "cd server/ && npm install",
    "install-webapp": "cd webapp/ && npm install",
    "install": "npm run install-server && npm run install-webapp",
    "start-server": "cd server/ && npm start",
    "start-webapp": "cd webapp/ && npm start",
    "fireloop": "fireloop"
  },
  "keywords": [
    "fireloop",
    "Grand Lyon"
  ],
  "author": "Clever Age",
  "license": "MIT",
  "devDependencies": {
    "@mean-expert/fireloop": "^1.0.0-beta.2.7"
  }
}
